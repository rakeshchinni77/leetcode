class Solution {
public:
    long long sumAndMultiply(int n) {
        if(n==0) return 0;
        string s = to_string(n);
        long long x = 0;
        long long sum =0;
        for(int i=0;i<s.size();i++) {
            if(s[i]-'0'!=0) {
                x = x*10+(s[i]-'0');
                sum+=s[i]-'0';
            }
        }
        return 1LL*(sum*x);
    }
};
