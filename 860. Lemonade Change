class Solution {
public:
    bool lemonadeChange(vector<int>& bills) {
        int n = bills.size();
        int f =0,t = 0;
        for(int i=0;i<n;i++) {
            if(bills[i]==5) f+=1;
            if(bills[i]==10) {
                if(f>0) {
                    f-=1;
                    t+=1;
                }
                else return false;
            }
            if(bills[i]==20) {
                if(t>0 && f>0) {
                    f-=1;
                    t-=1;
                }
                else if(f>=3) f-=3;
                else return false;
            }
        }
        return true;
    }
};
