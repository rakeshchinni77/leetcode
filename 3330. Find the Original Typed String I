class Solution {
public:
    int possibleStringCount(string word) {
        int cnt=1;
        int n=word.size();
        for(int i=0;i<n-1;i++) {
            if(word[i]==word[i+1]) {
                cnt+=1;
            }
        }
        return cnt;
    }
};
